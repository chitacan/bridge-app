# adb bridge server

`node.js` powered bridge server.

## prerequisites

* `node.js`
* `bower` (via `node.js`)

## setup

```
$ npm install && bower install
$ npm start
```

see `http://localhost:3000` on your browser.
